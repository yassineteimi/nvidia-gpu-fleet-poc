# Copy to .env and fill in. .env is gitignored and must never be committed.

# Scaleway API credentials, from https://console.scaleway.com/iam/api-keys
SCW_ACCESS_KEY=""
SCW_SECRET_KEY=""
SCW_DEFAULT_PROJECT_ID=""
SCW_DEFAULT_ORGANIZATION_ID=""

# Zone the cluster runs in. fr-par-2 is primary, pl-waw-2 is the fallback
# when L4 stock is unavailable.
SCW_DEFAULT_ZONE="fr-par-2"

# Public SSH key registered on both nodes.
SSH_PUBLIC_KEY_PATH="$HOME/.ssh/id_ed25519.pub"
SSH_PRIVATE_KEY_PATH="$HOME/.ssh/id_ed25519"

# Container registry for the gpu-remediator image (Session C).
GHCR_USER="yassineteimi"
GHCR_TOKEN=""
